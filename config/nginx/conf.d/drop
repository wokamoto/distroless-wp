location ~ /\.(?!well-known).* {
    deny all;
}

location ~* ^/wp-(admin/.*|includes/.*|content/.*|wp-.*\.php)$ {
    return 404;
}

location ~* (/(?:uploads|files)/.*\.php|.*\.(cache|sql|log|ba?k|org))$ {
    return 404;
}
